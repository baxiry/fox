
enum Status {
    Active{id int}
    Inactive{reason int}
}

fn main() {
    var s *Status = Status.Inactive{reason: 0}

    match s {
    case Active : 
        printf("Status is active with ID: %d\n", s.id)

    case Inactive: 
        printf("s in inactive %d\n", s.reason)

    }
}

