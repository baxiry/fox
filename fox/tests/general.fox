package testfox 

import(
   fmt 
   os 
   io 
)


type X struct {
    a int
    b int
}

type Obj struct {
    value int
}

var obj Obj
var a int
var b int = 10
var c = 10 + 10
var d = 10 + (8*10)

fn Vars() {
	var obj Obj
	var i int
	var i int = 10
	var i = 10 + 10

    name := "adam"
    age = 34
    info  = "someInfo" 
}

fn access() {

    user := User
    user.name = name
    user.age = age
    user.info = info

}

fn Call(name Name, age Age, info *Info) User {

    do(a,b,c)
    ptrfunc(*a,*b,*c)
    multifunc(a,b,*c)

    return user 
}

fn make() Obj {
    o := Obj
    return o
}

fn parent() {
    x := make()
    p := &x   
}

fn multiDefAss() {
   u, o := User{name:"adam",age: 23}, Obj{class: "A", size:90}
   u, o = do() 
   x, y := do()
   _ , y = do()
   x , _ = do()
   _,_ = do()
   g, _ := do()
   _, h := do()
}

fn worker(p *Obj) {}

fn parallel() {
    x := Obj
    spawn worker(&x)


    spawn a
    spawn a + b
    spawn &x
}


fn grops() {
    a * (b + c)
    (a + b) * c
    a + (b * c)

    a == b || (c != d && e < j)
}

fn compar() {
   t := true
   f := false

   c := t == f
   d := c != t

    a + b * c
    a * b + c
    a == b && c
    a & b == c
    &a + b

   a == b && c || d
}

fn Loops(x X, y Y, z *Z) Data, int  {

    for i:=0; i<10; i = i-1 {
       data.x = i
    }

    for i:=0; i<10; i = i+1 {
       data.x = i
    }

    i:=0
    for ; i<10; i = i+1 {
       data.x = i
    }

    for i:=0; ; i=i+1 {
       data.x = i
       if i == 10 {
           break
       }
    }

    for i:= 0; i <10; {
       data.x = i
       i =i+ 1
    }

    if i == 10 {
        i= i + 1
        if i == 100 {
            return data, 100
        }
    } 

    for {
       data.x = i
       if data.x > 100 {
            break
       }
    }

    ok := true
    for ok {
       data.x = i
       if !ok {
           break
       }
    }
    
    for {}

    return data, 30 
}

fn multiDefAss() {
   u, o := User{name:"adam",age: 23}, Obj{class: "A", size:90}
   u, o = do() 
   x, y := do()
   _ , y = do()
   x , _ = do()
   _,_ = do()
   g, _ := do()
   _, h := do()
}

