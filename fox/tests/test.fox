package testfox 

import(
   fmt 
   os 
   io 
)

type X struct {
    a int
    b int
}

type Obj struct {
    value int
}


fn Update(name Name, age Age, info *Info) User {

    name  = "adam"
    age = 34
    info  = "someInfo" 

    user := User

    user.name = name
    user.age = age
    user.info = info

    do(a,b,c)
    ptrfunc(*a,*b,*c)
    multifunc(a,b,*c)


    return user 
}

fn make() Obj {
    o := Obj
    return o
}

fn parent() {
    x := make()
    p := &x   
}

fn worker(p *Obj) {
}

fn bad() *Obj {
    o := Obj
    return &o    
}

fn bad2(o *Obj) *Obj {
    return &o    
}

fn parallel() {
    x := Obj
    spawn worker(&x)
}

fn Loops(x X, y Y, z *Z) Data, int  {
    data := Data

    for i:=0; i<10; i = i+1 {
       data.x = i
    }
    i:=0
    for ; i<10; i = i+1 {
       data.x = i
    }
    for i:=0; ; i = i+1 {
       data.x = i
       if i == 10 {
           break
       }
    }
    for i:= 0; i <10; {
       data.x = i
       i =i+ 1
    }
    return data, 30 
}
