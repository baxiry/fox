
type User struct {
    id int 
    padding [256]int
}

fn main() {
    // A loop to aggressively allocate memory and trigger the micro-heap threshold
    var i int
    for i = 0; i < 1000000; i = i + 1 {
        var u *User = User{id: 123}
    }
}
