
type User struct {
    name string
    age  int
}

fn main() {
    // A loop to aggressively allocate memory and trigger the micro-heap threshold
    var i int
    for i = 0; i < 200; i = i + 1 {
        var u *User = User{name: "Test", age: i}
    }
}
